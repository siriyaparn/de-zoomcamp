import sys
import pandas as pd

print(sys.argv)
day = sys.argv[1]  # the first argument passed to the script after the script name

# some funcy stuff with pandas
print(f"Job finished successfully for day {day}")